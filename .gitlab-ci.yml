image: node:latest

stages:
  - test

cache:
  paths:
    - node_modules/

install_dependencies:
  stage: test
  script:
    - npm ci

run_tests:
  stage: test
  parallel: 1
  script:
    - npm run test
